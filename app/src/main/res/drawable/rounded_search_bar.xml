<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="#FFFFFF"/> <!-- Color de fondo -->
    <corners android:radius="20dp"/> <!-- Esquinas redondeadas -->
    <stroke
        android:width="1dp"
        android:color="#E0E0E0"/> <!-- Color del borde -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/> <!-- Espaciado interno -->
</shape>
